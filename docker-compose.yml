services:
  pyex-eval:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4000:4000"
    environment:
      - MIX_ENV=dev
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4000/run"]
      interval: 5s
      timeout: 3s
      retries: 5
